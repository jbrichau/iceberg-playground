true == []; // -> false
true == ![]; // -> false

toNumber(true); // -> 1
toNumber([]); // -> 0


1 == 0; // -> false
true == ![]; // -> false

![]; // -> false

true == false; // -> false

"b" + "a" + +"a" + "a"; // -> 'baNaNa'

"I made these changes and put them in the staging area"